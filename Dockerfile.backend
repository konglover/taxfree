# 后端 Dockerfile
FROM node:18-alpine

WORKDIR /app

# 复制 package 文件
COPY backend/package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY backend/ .

# 创建数据目录
RUN mkdir -p data

# 暴露端口
EXPOSE 3000

# 启动应用
CMD ["node", "src/index.js"]






